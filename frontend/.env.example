DATABASE_URL="postgresql://postgres:postgres@localhost:5432/maxconect"
NEXT_PUBLIC_APP_NAME="Maxconect Crm"

# Usuario inicial (usado apenas se nao existir nenhum user na base)
AUTH_DEFAULT_EMAIL="admin@maxconect.local"
AUTH_DEFAULT_USER="Administrador"
AUTH_PASSWORD="NovaSenha123" # minimo 8, com letras e numeros
# AUTH_PASSWORD_HASH="$2a$10$..." # opcional, tem prioridade sobre AUTH_PASSWORD

# Autenticacao
AUTH_JWT_SECRET="troque-por-uma-chave-longa"
AUTH_ALLOWED_EMAILS="admin@maxconect.local"
AUTH_DEV_BYPASS="false"

# Cron de follow-ups
CRON_SECRET="troque-por-uma-chave-longa"

# Rate limit via Upstash Redis (opcional)
# UPSTASH_REDIS_REST_URL="https://SEU_ENDPOINT_REST"
# UPSTASH_REDIS_REST_TOKEN="SEU_TOKEN"
